<%- include ('partials/header.ejs') %>

    <body>
        <%- include ('partials/navbar.ejs') %>
            <div class="container">

                <% if (erros) { %>
                    <div class="mostrar-erros mt-2  ">
                        <% erros.erros.forEach(erro=> { %>
                            <div class="animate__animated animate__fadeIn alert alert-danger mt-2 mb-0" role="alert">
                                <%= erro %>
                            </div>
                            <% }) %>
                    </div>
                    <% } %>


                        <div class="card mt-3    " id="formulario-pergunta">
                            <!-- Card Header -->
                            <div class="card-header">
                                <h3>Realizar pergunta</h3>
                            </div>
                            <!-- Card Body-->
                            <div class="card-body">
                                <!-- Formulário de pergunta-->
                                <form method="POST" action="/salvarpergunta">
                                    <label>Título</label>
                                    <input type="text" name="titulo" placeholder="Título" class="form-control">
                                    <br>
                                    <label>Descrição</label>
                                    <textarea name="descricao" placeholder="Descreva sua pergunta aqui"
                                        class="form-control"></textarea>
                                    <br>
                                    <button class="btn btn-success">Concluir</button>
                                </form>
                            </div>
                        </div>
            </div>
    </body>
    <script>
        function validar() {
            let titulo = formulario.nome
        }
    </script>
    <%- include ('partials/footer.ejs') %>